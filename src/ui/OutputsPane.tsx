/**
 * Center pane — KPI tiles and charts.
 * Phase 0/2 stub: shows loading state.
 * KPI tiles in Phase 2. Full charts in Phase 3.
 */

export function OutputsPane() {
  return (
    <main className="h-full overflow-y-auto bg-white p-6">
      <h2 className="text-sm font-semibold text-gray-700 uppercase tracking-wide mb-4">Outputs</h2>
      <p className="text-xs text-gray-400 italic">KPI tiles and charts implemented in Phase 2–3.</p>
    </main>
  )
}
